# Class-loading is read before normal config (Quarkus docs): must be in application.properties,
# not only via quarkus.config.locations. Keeps Aidsfuscator hashIntegrity on sdk-bundle working.
quarkus.class-loading.parent-first-artifacts=io.backbone.core:sdk-bundle

quarkus.http.test-port=0

# disable all Quarkus dev services
quarkus.devservices.enabled=false

# disable audit publishing in tests (no audit-service calls or secrets)
backbone.audit.enabled=false

# Do not set security.test.bypass-auth — ActorBffIT asserts unauthenticated → 401.
